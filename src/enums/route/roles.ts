export enum UserAccountStatusTypeEnum {
  unregistered = 0, // 未注册状态
  unaudited = 1, // 未审核状态
  forbidden = 2, // 禁用状态
  default = 3, // 正常状态
}
export enum RouteCodeTypeEnum {
  default = 0, // 默认角色
  developer = 1, // 开发角色
  editor = 2, // 编辑角色
  admin = 3, // 管理角色
}
